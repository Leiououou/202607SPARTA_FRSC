# DS2V 表面复合讨论记录

日期：2026-07-28

## 论文依据

- 本地论文：`D:\博一\气固相互作用\复合系数\main.pdf`
- 关键位置：PDF 第 6 页、期刊页码 2830。
- 标黄副本：`output/pdf/main_highlighted.pdf`

论文明确说明：

- DS2V 按预先给定的概率执行表面复合反应。
- 论文把概率设为 1 和 0，分别模拟完全催化和非催化表面。
- 第一个分子撞击后停留在表面；第二个分子撞击同一表面微元时，
  如果能与第一个分子复合便生成分子并返回流场。
- 如果不能复合，两个分子分别离开表面。
- 发生复合时，论文规定全部反应能传给表面。
- 总热流写成对流热流与反应热流之和。
- 论文采用的五条表面反应为：
  `O+O->O2`、`N+N->N2`、`N+O->NO`、`CO+O->CO2` 和
  `C+O->CO`。

## 当前较强推断

- 对纯的、反应物相容的入射流，如果连续两个原子配对后以概率
  `P` 反应，则按原子消耗定义的复合系数满足 `gamma=P`。
  这与论文用 `P=1` 表示完全催化一致。因此，之前关于
  `P=gamma/2` 的猜测不适用于这种配对解释。
- 竞争反应主要表现为局部的入射配对竞争：第二个到达同一微元的
  物种决定可能进入哪条反应通道。

## 论文尚未说明，不能当成事实

- 每个表面微元究竟只保存一个等待模拟粒子，还是能保存多个粒子
  或各物种累计数。
- 等待粒子是否存在超时释放，是否在时间步或采样周期边界清空。
- 同一反应物对具有多个产物通道时，随机通道选择算法如何实现。
- MPI 分区下等待表面状态如何归属、迁移或同步。

## 表面网格影响

- 只要要求两个粒子撞击同一表面微元，表面离散就参与了反应算法，
  所以必须进行表面网格敏感性验证。
- 如果等待没有超时，均匀纯组分在充分长时间统计下，平均反应率
  可能基本不依赖网格；网格主要影响未配对余数、等待时间和噪声。
- 表面微元过大会混合空间上相距较远的局部入射通量。
- 表面微元过小会减少每个微元的撞击样本，增大等待时间和统计噪声；
  如果存在超时或周期性清空，还可能直接降低反应率。
- 这篇论文使用轴对称 DS2V。二维母线上的表面线段代表真实三维
  环带面积，并不等于一个真实吸附位点。

## 与 n_site 的关系

- 不能把 DS2V 直接等同于某个真实物理位点密度。
- 即使源码最终证实“每个微元一个等待槽”，也只能称为一个数值
  配对槽，不能直接视为固定材料属性 `n_site`。
- 当前 SPARTA gamma 模型包含长期库存、覆盖率和 `n_site`，因此
  不能自动视为 DS2V 的复刻。
- 若以后实现严格的 DS2V 风格，应考虑局部入射配对、预设反应概率、
  失败时两个粒子分别释放，以及不引入物理位点饱和；具体细节仍需
  DS2V 源码或更详细手册确认。
